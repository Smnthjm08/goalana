# Goalana — Project Checklist

Last updated: 2026-07-18. Typecheck (6/6) clean.

**Status note:** `docs/IMPLEMENTATION_PLAN.md` and `docs/MARKET_LIFECYCLE.md` describe several items below (lock automation, settlement, claims) as "missing." That's stale — they're implemented in the current working tree. This file reflects actual current state, verified by running the code, not by reading older docs.

## Done and verified

- [x] Fixture ingestion — snapshot (hourly + boot + manual `/api/fixtures/sync`) and delta updates (5 min)
- [x] Odds ingestion — snapshot sync + live SSE stream (`odds.worker.ts`, upgraded from polling)
- [x] Scores ingestion — live SSE stream (`scorer.worker.ts`), canonical live-state derivation, `finalSeq` detection
- [x] Scores historical backfill — **was broken (SSE-text-as-array bug), now fixed and verified**
- [x] Fixture/odds/markets REST API (`GET /api/fixtures`, `/api/fixtures/:id`, `/api/fixtures/:id/odds/history`)
- [x] Frontend fixture list + detail page, odds movement chart, live score header, match event timeline
- [x] Wallet connect (`@solana/wallet-adapter-*`, devnet, autoConnect)
- [x] Automatic wallet → user registration/recognition (`POST /api/users/connect`, upsert by wallet address)
- [x] Anchor program: config/market/vault/position state, all PDAs verified byte-for-byte against the SDK
- [x] `create_market` (house-only) — verified idempotent, on-chain + DB mirror consistent for all 9 real markets
- [x] `place_bet` — verified on real devnet: single + accumulated bets, pool totals, position updates, lock rejection
- [x] `lock_market` / `cancel_market` — implemented, localnet-tested, exercised live during bet validation
- [x] `settle_market` (CPI Merkle proof verification) — implemented, localnet-tested (settlement itself out of this pass's scope)
- [x] `claim_winnings` / `claim_refund` — implemented, localnet-tested (out of this pass's scope)
- [x] Backend lock automation (`lock.service.ts` + `lifecycle.cron.ts`, every 1 min)
- [x] Backend settlement automation (`settlement.service.ts`, same cron) — fetches TxLINE proof, calls `settle_market`
- [x] Frontend bet UX — loading/success/error toasts, disabled-while-pending, disabled-on-locked/settled, position display, pool totals
- [x] Frontend claim UX — claim winnings/refund buttons, payout preview, settlement-proof display panel
- [x] Anchor localnet test suite — 26/26 passing (PDAs, create/lock/cancel, settle incl. CPI mock + edge cases, place_bet + accumulation, claim winnings + refund incl. empty-pool path)
- [x] Monorepo typecheck + build — all 8 packages clean

## Known gaps / not yet done

- [ ] On-demand market creation endpoint (`POST /api/fixtures/:id/markets`) — today market creation is cron-only (every 10 min, first 10 upcoming fixtures); no way to force-create for one fixture without waiting. Worked around manually this pass via a one-off script; still worth building as a real endpoint.
- [ ] Recurring full fixture-snapshot resync exists now (hourly) — confirm this is sufficient or needs tightening before a live tournament day
- [x] `apps/web/app/dummy/page.tsx` — already deleted (staged in git)
- [ ] `odds.cron.ts`'s `syncOdds()` re-fetches odds for every tracked fixture including finished ones — works, just more TxLINE calls than needed; add a time-window filter if call volume becomes a cost concern
- [x] Settlement/claim flow — **live-devnet validated this pass**, see below for what was found and fixed.
- [x] `docs/TXLINE.md` / `docs/ARCHITECTURE.md` odds-is-poll-based claim — fixed (both now correctly describe the SSE-based `odds.worker.ts`); `TXLINE.md` also got a pass against TxLINE's official docs (verified via WebFetch of `txline.txodds.com/documentation/*`): added network/credential tables, subscription tiers, all three validation PDA types (Goalana only uses `daily_scores_roots`), a troubleshooting cross-reference, and corrected two more stale claims (`scores/stat-validation` and `settle_market` wiring — both are implemented, doc said otherwise)
- [ ] `docs/IMPLEMENTATION_PLAN.md` / `docs/MARKET_LIFECYCLE.md` status tables are stale (mark lock/settlement/claims as missing when they're implemented) — needs a refresh pass
- [x] Dedicated devnet RPC provider — **configured this pass.** Helius devnet URL was added to `.env` as `HELIUS_RPC_URL`/`HELIUS_RPC_KEY`, but nothing in the codebase reads those var names — `SOLANA_RPC_URL` (backend, `goalana.service.ts`) and `NEXT_PUBLIC_SOLANA_RPC_URL` (frontend, `solana-provider.tsx`) are what's actually consumed, and both were still pointing at the public `api.devnet.solana.com`. Fixed by pointing both at the Helius URL directly; confirmed live via a real `getHealth`/`getBalance` RPC call. `.env.example` documents this (auto-regenerated by the project's env-sync tooling once `.env` changed). Two dead-end scripts (`apps/api/src/scripts/activate.ts`, `packages/goalana-sdk/scripts/initialize-config.ts`) still hardcode the public RPC — neither is part of the running service (manual one-offs, `initialize-config.ts` isn't referenced anywhere), so left as-is.
- [x] `InvalidBetAmount` (zero-stake) on-chain guard — still code-inspection only; not re-attempted this pass (out of this pass's scope, see task list below).

## Live-devnet settlement/claims validation (2026-07-16)

Ran `settleFinishedFixtures()` for real against the two fixtures that had actually finished (France v Spain, England v Argentina — real World Cup 2026 matches, real TxLINE `finalSeq`/stat data). Found a real bug, confirmed a prior fix, and fully validated the claims path:

- **Found:** all 9 markets on those two fixtures failed on-chain with `StaleOracleSnapshot`. Root cause: `settleAfter` is baked immutably into the Market account at creation time. These 9 were created with an old buffer (~kickoff+3h); real match-finalization timestamps land ~2h post-kickoff, so the on-chain check (`oracle_ts_secs >= market.settle_after`) could never pass. `market.service.ts:288-303` shows this exact issue was already diagnosed and fixed for markets created *going forward* (now `locksAt + 15min`), but the fix isn't retroactive — settle_after can't be changed after creation.
- **Resolved:** cancelled all 9 stuck markets on-chain (`cancel_market`, authority-gated, unconditional) — they had $0 in either pool, so no funds were at risk. DB synced to `CANCELLED`. They would otherwise have sat at `LOCKED` forever.
- **Claims fully validated end-to-end on real devnet:** since neither upcoming semifinal (France v England, Spain v Argentina — both ~2-3 days out at the time) had TxLINE-priced full-time odds yet, created one on-chain market with a synthetic (never-true) predicate tied to a real fixture, then ran `create_market` → `place_bet` (0.02 SOL) → `cancel_market` → `claim_refund` — all four real transactions succeeded. Confirms the full claim path works; DB row for the synthetic market was deleted afterward so it doesn't show in the real fixture's market list.
- **Not yet validated:** `claim_winnings` specifically (the win-payout math path) — only `claim_refund` was exercised live. Needs a market that actually settles (not cancels) with a real bet on the winning side; blocked on TxLINE pricing the upcoming semifinals' full-time markets, or a later real fixture.

## Demo polish (2026-07-16)

- [x] Lifecycle status strip — built `apps/web/components/fixtures/lifecycle-status-strip.tsx` (Scheduled → Live → Finished → Settled stepper, or a Cancelled banner), wired into the fixture detail page above the tabs. Verified against real API response shapes for both a fully-cancelled fixture and an upcoming empty-markets fixture.
- [x] Settlement-proof display — already existed (`fixtures/[fixtureId]/page.tsx`'s "Settlement Proof" panel), no change needed.
- [x] `/dummy` cleanup — already done (deleted, staged).
- [x] Stale-doc refresh — `docs/TXLINE.md`/`docs/ARCHITECTURE.md` odds claims fixed (see above).

**Incident note:** while verifying the lifecycle strip, an `rm -rf .next` collided with an already-running `turbo run dev` (started outside this session, with a live browser tab connected) and corrupted its build cache; the follow-up process kill fully took the dev server down before it could be restarted. Restarted cleanly via `turbo run dev` from repo root — confirmed healthy (both `apps/web` on :3000 and `apps/api` on :8081 serving 200s, single process tree, no orphans). No data was lost; the disruption was limited to a dev-server restart blip.

## Full lifecycle validation — Helius cutover + Devnet/Localnet split (2026-07-16)

**RPC cutover.** Dev environment had **three separate `apps/api` processes running concurrently** before this pass started (PIDs with start times 01:02, 20:20, and 21:20 — the oldest had been running unsupervised for 20+ hours, none tracked by the current `turbo run dev` tree). All were on the stale public RPC and could have been double-processing the same lifecycle cron ticks. Killed all three plus the web tree, confirmed ports 3000/8081 fully free, restarted a single clean `turbo run dev`. Verified: exactly one process per service, both ports serving 200s, Helius RPC reachable (`getHealth` + a real `getBalance` call against the backend wallet), fixture/odds sync and TxLINE SSE streams (odds + scores) connected with no errors in logs.

**Devnet — real fixture, corrected `settleAfter`.** TxLINE had, by this point, priced full-time markets for the France v England semifinal (fixture `18257865`, kickoff 2026-07-18T21:00Z — confirmed real via `discoverMarketsForFixture` returning 5 supported market types from live odds). Created all 5 real markets via `create_market`, using the corrected `settleAfter = locksAt + 15min` logic:

| Market | PDA | create_market tx |
|---|---|---|
| FULL_TIME_HOME_WIN | `GRzRon4Pf3SQUjQL61LPCxE5fmpRX1qmz1E1vZpmTwfz` | `g5ErxKVHGdGCwwqapzfj3SB9AR9kZ5p3FW8sxESiLRek938iYayGPio8NLTpLY5gotDRbQaaZvJCAhxF3tZRq1n` |
| FULL_TIME_DRAW | `AuVam6DqcwEqQQJYL9Ke8AUBUrfmyWakgRKX82ocZHbs` | `qKMY84JxFw1biw5SNgwpzkMFZJRtJHevdkUCEuxjvSLyaysL98FGEtEmzqDD7HQFM2A5H3aBbo2spXrwsDDdWVy` |
| FULL_TIME_AWAY_WIN | `BEGo5bbpdGV9QHViMPg1juYdNFzUhStfNCJCEVZEQ948` | `Eg5yJqpseHRiuhccxRytCRVaqZzZXpf923U7CswM5ArPWpBMZ9gCPxntZknYCkP859mfRPt8CJ8A7MQHgJQfVwa` |
| FULL_TIME_OVER_1_5 | `C4ZmEJgumWEbr2BUqAWiFr7gkdPujg3tEZoLEc1DLo5F` | `3eLnsqqrqQa8KgdpWe4Jt7qYNnRm9m5cnUqN8jcoGnkcekezFzFE2J5AhoPVf2GmoE8jmWdEzSmjeg3N9Zk84r2a` |
| FULL_TIME_OVER_3_5 | `XtHHXLNidVE691x7Wkxknefxp2HAJzgMQHNCeRu3gcd` | `2dAgWsdAQ3PmFr8kNrnqw4xSLRrcGChu2GkG3hdbkoPvgixYkjve2iPucPWR8ZRtGM2DCEe9HQjNshG6KUqzfQxG` |

(Explorer: `https://explorer.solana.com/tx/<sig>?cluster=devnet` or `/address/<pda>?cluster=devnet`.)

On-chain read-back of `FULL_TIME_HOME_WIN` confirmed: `status=open`, `locksAt=2026-07-18T21:00:00Z`, `settleAfter=2026-07-18T21:15:00Z` — exactly `locksAt+15min`, proving the fix is correctly applied to newly-created markets. `totalYes`/`totalNo` both 0, `outcome=null` pre-bet, matching a freshly-created market.

Placed a real bet: `place_bet` (0.05 SOL, YES) on the HOME_WIN market — tx `5KNNajkrmw51Gas72vR9KaeB42TK3pW3xpbcyokk3cW9tzTqErM5m2Q8NRFZc57i59UJ2vpVyu9kU3iPwSykNpYq`. Re-read on-chain state after: `totalYes=50000000` (0.05 SOL, lamports), Vault PDA `3wHApPpeqVaQnYW8bsNFtJmqg4qgH2jSM43dkf3iwovf` created lazily on first bet as expected, holding `50953520` lamports (bet + rent-exempt reserve), owned by the goalana program. Confirms escrow/vault accounting is correct.

**Why lock/settlement/claim_winnings weren't also forced on this devnet market:** `locksAt` is the real kickoff, 2026-07-18T21:00Z — 2 days out, so the *automatic* cron-triggered lock (`lock.service.ts`, checked live: "0 markets due to lock" while `locksAt` is still future — correct behavior, not a bug) can't be observed within this session. `lock_market.rs` has **no on-chain time check at all** — locking is purely authority-gated, `locksAt` is informational/off-chain only — so a manual early lock *would* have succeeded, but deliberately wasn't done: it would pull one of 5 real markets out of live betting for the actual semifinal for no real validation benefit, since the exact same instruction is already exhaustively covered on localnet with a controllable clock. Settlement is even less forceable live — no real match-finalization proof exists for a match that hasn't been played. Left all 5 markets fully OPEN for real demo betting.

**Localnet — full lock/settle/claim/payout/treasury coverage.** Ran the existing Anchor test suite (`goalana_program/tests/index.ts`, `anchor test`), after adding one assertion (not a new feature — a regression check) to the existing `"succeeds to claim winnings proportionally"` test: a second `claimWinnings()` call for the same already-paid user, asserted to fail with `AlreadyClaimed`. **26/26 passing**, full log in `/tmp/anchor-test.log`. Coverage includes, with real on-chain balance-diff assertions (not just event/status checks):

- `lock_market`: Open→Locked success, Locked→Locked rejection, Cancelled→Locked rejection, unauthorized-signer rejection.
- `settle_market`: rejects before `settle_after`, wrong fixture ID, wrong stat key, wrong daily-root PDA, already-settled, cancelled market, and **stale oracle timestamp** (the exact same `StaleOracleSnapshot` class of bug found live on devnet earlier this pass, here exercised under a controlled clock) — plus real outcome=true and outcome=false paths via the CPI mock.
- `claim_winnings`: 3-user proportional payout (2 SOL / 1 SOL / 3 SOL stakes, real balance diff confirms winner receives exactly `1 × 6 / 3 = 2 SOL`), **double-claim now explicitly rejected** (new assertion, `AlreadyClaimed`), losing bettor rejected (`NoWinningStake`).
- `claim_refund`: cancelled-market refund, and settled-market-with-empty-winning-pool refund.

## Next milestone (recommended order)

1. Validate `claim_winnings` live on Devnet (not just localnet) — needs a real bet on the winning side of a market that actually settles. The 5 France v England markets created this pass are positioned for exactly this once the real match happens (2026-07-18) and, assuming a lifecycle-cron-running instance is still up then, lock/settle should fire automatically — revisit after that date. **Still open as of 2026-07-18 evening** — kickoff is 21:00 UTC today, match hasn't happened yet.
2. Build the on-demand market creation endpoint (`POST /api/fixtures/:id/markets`) — this pass again needed a one-off script to force-create ahead of the 24h cron window. **Still open** — re-checked `apps/api/src/index.ts`, no such route exists yet.
3. `docs/IMPLEMENTATION_PLAN.md` / `docs/MARKET_LIFECYCLE.md` stale-status refresh pass. **Still open.**
4. Point `activate.ts` / `initialize-config.ts` at the Helius RPC too if they're ever going to be run again (currently hardcoded to the public endpoint, but both are dead/manual scripts, not urgent). **Still open, still not urgent.**

## 2026-07-18 — three more commits, reviewed here

Full detail lives in v2-todo.md's and v3-todo.md's 2026-07-18 progress-log entries; summary for this file's audit trail:

- **`869f520`** reverted the multi-competition Devnet hedge added the previous pass (`apps/api/src/config/competition.ts`, its diagnostic scripts, README's "Validation mode" section) — back to a single `WORLD_CUP_COMPETITION_ID` constant. This also trimmed `todo.md` itself (the "Multi-competition Devnet validation infrastructure" section this file used to carry has been removed by that commit, since it documented infra that no longer exists).
- **`64e6664`, `54b88ab`** added shareable betting/market/fixture links with OpenGraph previews, a new `GET /api/markets/:marketPda` endpoint, and dedicated market/position detail pages. Genuinely new, working, typechecked functionality — not something either todo doc had queued, and adjacent to a feature v2-todo.md's competitor teardown had flagged as a non-goal (share-on-X). Not itself a correctness concern, just flagged for awareness.
- **Repo-state audit while reviewing these commits:** working tree is clean and `main` (GitHub's confirmed default branch) is fully pushed to `origin/main` — no uncommitted work sitting around. **However, `gh repo view` reports the repository visibility as `PRIVATE`** — a judge cannot clone or browse a private repo. This is now the most urgent open item across all three todo docs (see v3-todo.md P0-3) and isn't captured anywhere above since it's new information, not a code gap.


